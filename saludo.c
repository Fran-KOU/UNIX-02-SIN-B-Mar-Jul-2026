#include <stdio.h>  // Se incluye una librería.

int main() { // Se declara la variable int para que acepte el numeros enteros, y "main" quiere decir que todo este bloque de codigo desde la llave se ejecutará primero. Todo el bloque de codigo se separa con llaves({})
    printf("Hello World!\n"); // Se hace print o imprimir en la terminal
    return 0; // Se retorna 0 si todo sale bien, de lo contrario se retornará -1
} // Aquí acaba el bloque de código.

// ELF = .exe
